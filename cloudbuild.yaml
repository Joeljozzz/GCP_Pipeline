steps:
  # Step 1: Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/ethereal-fort-435120/my-first-project:$_COMMIT_SHA', '.']

  # Step 2: Push Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'us-central1-docker.pkg.dev/ethereal-fort-435120/my-first-project:$_COMMIT_SHA']

options:
  logging: CLOUD_LOGGING_ONLY
